# Public URL of the FastAPI backend (no secrets here: this value is embedded in the browser bundle).
# Leave empty for local development: the app then uses http://<same host>:8000.
NEXT_PUBLIC_BACKEND_URL=
